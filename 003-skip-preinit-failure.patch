--- /tmp/base.c.orig	2026-03-31 06:04:25.189597340 -0400
+++ /tmp/nouveau/nvkm/engine/device/base.c	2026-03-31 05:56:47.091565769 -0400
@@ -3041,8 +3041,10 @@
 	s64 time;
 
 	ret = nvkm_device_preinit(device);
-	if (ret)
-		return ret;
+	if (ret) {
+		nvdev_error(device, "preinit failed (%d), continuing anyway for GSP boot...\n", ret);
+		/* Don't return - continue to GSP boot despite preinit failure */
+	}
 
 	nvkm_device_fini(device, NVKM_POWEROFF);
 
