--- /tmp/nouveau_orig_nvkm_subdev_gsp_rm_r535_gsp.c	2026-03-31 07:40:52.262895458 -0400
+++ /tmp/linux-nouveau/drivers/gpu/drm/nouveau/nvkm/subdev/gsp/rm/r535/gsp.c	2026-03-31 07:23:06.201033062 -0400
@@ -1818,8 +1818,8 @@
 	int ret;
 
 	ret = nvkm_gsp_fwsec_sb_ctor(gsp);
-	if (ret)
-		return ret;
+	/* PATCH: skip fwsec for recovery */
+	if (ret) { nvkm_error(&gsp->subdev, "fwsec init failed (%d), continuing\n", ret); ret = 0; }
 
 	hdr = nvfw_bin_hdr(&gsp->subdev, fw->data);
 	desc = (void *)fw->data + hdr->header_offset;
